import styled from "@emotion/styled";
import { CSS_CARD } from "../../../../styles/shared";
import { FC } from "react";

const StatCard: FC = () => {
  return <Holder>asdfas</Holder>;
};

export default StatCard;

const Holder = styled.div`
  ${CSS_CARD};
  padding: 23px;
`;
